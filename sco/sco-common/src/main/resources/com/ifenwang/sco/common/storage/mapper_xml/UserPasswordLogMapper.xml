<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ifenwang.sco.common.storage.mapper.UserPasswordLogMapper">
	
	<sql id="column">
		USERID, 
		LOGINCOUNT, 
		LOGINTIME
	</sql>
	
	<insert id="save" parameterType="UserPasswordLog">
		insert into TL_USERPASSWORDLOG (
			userId, 
			loginCount, 
			loginTime)
		values (
			#{userId,		jdbcType=VARCHAR}, 
			#{loginCount,	jdbcType=INTEGER},
			#{loginTime,	jdbcType=DATE})
	</insert>
	
	<update id="update" parameterType="UserPasswordLog">
		update TL_USERPASSWORDLOG
		<set>
			<if test="loginCount != null">
				loginCount = #{loginCount,jdbcType=INTEGER},
			</if>
			<if test="loginTime != null">
				loginTime = #{loginTime,jdbcType=DATE},
			</if>
		</set>
		where userId = #{userId,jdbcType=VARCHAR}
	</update>
	
	<delete id="deleteByUserId" parameterType="java.lang.String">
		delete from TL_USERPASSWORDLOG where userId = #{userId, jdbcType=VARCHAR}
	</delete>
	
	<select id="queryByUserId" resultType="UserPasswordLog" parameterType="java.lang.String">
		select <include refid="column" /> from TL_USERPASSWORDLOG where USERID = #{userId, jdbcType=VARCHAR}
	</select>
	
</mapper>